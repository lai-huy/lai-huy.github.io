import React from "react";
import "./loading.scss";

const Layout = () => {
    return (
        <div id="progressbar">
            <span id="loading"></span>
            <div id="load">LOADING..</div>
        </div>
    );
};

export default Layout;
